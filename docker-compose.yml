services:
  tolgee:
    image: tolgee/tolgee:v3.183.5
    container_name: tolgee
    ports:
      - "8200:8080"
    volumes:
      - tolgee-data:/data
    environment:
      SERVER_PORT: 8080
    restart: unless-stopped

volumes:
  tolgee-data:
